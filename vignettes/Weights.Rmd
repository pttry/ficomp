---
title: "Weights"
output: rmarkdown::html_document
vignette: >
  %\VignetteIndexEntry{Labour_cost}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
# library(ficomp)
load_all()

library(dplyr)
library(ggplot2)
library(ggptt)

set_ptt()

data("wbis_narrow_dat")

```

# BIS weights narrow

```{r bis_nar}

wbis_narrow_dat %>% 
  tidyr::separate(time, into = c("time1", "time2"), sep = "_", convert = TRUE) %>% 
  filter(geo == "FI") %>% 
  mutate(weight = tidyr::replace_na(weight, 0)) %>% 
  mutate(geo_big = forcats::fct_lump_min(geo2, 30, w = weight)) %>%
  group_by(time1, geo_big) %>% 
  summarise(weight = sum(weight)) %>% 
  ungroup() %>% 
  mutate(geo_big = forcats::fct_reorder2(geo_big, time1, weight)) %>% 
  ggplot(aes(time1, weight, colour = geo_big)) +
  geom_line()



```

```{r}

wbis_narrow_dat %>% 
  group_by(time, geo) %>% 
  summarise(w = sum(weight, na.rm = TRUE)) %>% 
  pull(w) %>% 
  all(. == 100)

```

